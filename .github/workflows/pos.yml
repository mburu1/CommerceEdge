name: POS

on:
  workflow_call:
  push:
    paths:
      - 'pos/**'

jobs:
  build-test:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: pos/CommerceEdge.POS
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: pos/CommerceEdge.POS/package-lock.json
      - run: npm ci
      - run: npm run build
      - run: npm test
