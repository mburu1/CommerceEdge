# Tech Context — CommerceEdge

## Backend (.NET)
- C# / .NET — primary language and runtime
- ASP.NET Core — REST APIs
- Entity Framework Core — data access (general)
- Dapper — high-performance SQL/query scenarios
- SQL Server — primary transactional database
- Redis — distributed caching
- FluentValidation — request validation
- OpenTelemetry — observability
- Serilog — structured logging
- Swagger/OpenAPI + Scalar — API documentation
- xUnit — unit testing
- Testcontainers — integration testing

## POS
- TypeScript + Node.js + npm
- Request/Response patterns, Triggers, Operations, Views, Services

## Web Commerce
- React + TypeScript
- Vite — build tooling
- React Router — client-side routing
- TanStack Query — server-state management

## Infrastructure
- Docker + Docker Compose — containerization and local dev
- GitHub Actions — CI/CD (ci.yml, backend.yml, frontend.yml, security.yml)
- Prometheus — metrics
- Grafana — dashboards
- Jaeger — distributed tracing

## Solution File
`CommerceEdge.slnx` lives at the repository root and orchestrates all backend .NET projects.

## Repository Root
`d:\Mwangi Wa Mburu\Coding\CommerceEdge`
