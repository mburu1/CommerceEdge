services:
  sqlserver:
    image: mcr.microsoft.com/mssql/server:2022-latest
    environment:
      SA_PASSWORD: "${SA_PASSWORD}"
      ACCEPT_EULA: "Y"
    ports:
      - "1433:1433"
    volumes:
      - sqlserver-data:/var/opt/mssql

  redis:
    image: redis:7-alpine
    ports:
      - "6379:6379"

  rabbitmq:
    image: rabbitmq:3-management-alpine
    ports:
      - "5672:5672"
      - "15672:15672"

  api:
    build:
      context: ../..
      dockerfile: infra/docker/api/Dockerfile
    ports:
      - "5000:8080"
    environment:
      - ASPNETCORE_ENVIRONMENT=Production
      - ASPNETCORE_URLS=http://+:8080
      - Jwt__SecretKey=${JWT_SECRET_KEY:-local-development-secret-key-min-32-chars}
      - ConnectionStrings__DefaultConnection=Server=sqlserver;Database=CommerceEdge;User Id=sa;Password=${SA_PASSWORD};TrustServerCertificate=True
      - Redis__ConnectionString=redis:6379
    volumes:
      - api-logs:/app/logs
    depends_on:
      - sqlserver
      - redis

volumes:
  sqlserver-data:
  api-logs:
